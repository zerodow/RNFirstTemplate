import {DefaultTheme} from '@react-navigation/native';

export default {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    background: 'white',
    white: '#ffffff',
    black: '#000000',
    btn1: 'green',
    btn2: 'red',
  },
};
