const DATE_FORMAT = 'DD/MM/YYYY';
const TIME_FORMAT = 'hh:mm a';

const theme = {
  light: 'light',
  dark: 'dark',
};

const USER_DATA = 'userData';

export {DATE_FORMAT, TIME_FORMAT, theme, USER_DATA};
